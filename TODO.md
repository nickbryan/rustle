* Do we need nom parser? It looks like we could be fine without. Maybe for command mode parsing?
* How do we show the current chord / pending unput? Would this be an action and a render component for it?
* Add a way to have an input be a chord and an action so j = "action" && jj = "action".
* Add a way to have custom timeouts per binding.
* Add a way to have descriptions for bindings.