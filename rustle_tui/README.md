# rustle_tui

This crate provides a terminal-based user interface for the Rustle editor.

It uses the `crossterm` library as a backend to render the editor in the terminal.

## Running

This is a binary crate, so you can run it using `cargo run`:

```sh
cargo run --bin rustle_tui
```
